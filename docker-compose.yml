services:
  rbe_capstone:
    image: ghcr.io/sushiraj99/rbe-capstone:testing
    container_name: rbe_capstone
    # network_mode: host  # incompatible with port mapping on macOS; re-enable for robot comms
    ipc: host
    hostname: rbe-capstone-env
    environment:
      - RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
      # Virtual framebuffer display (Xvfb) for macOS — view via VNC on port 5900
      - DISPLAY=:99
      - LIBGL_ALWAYS_SOFTWARE=1
      - GALLIUM_DRIVER=llvmpipe
      - MESA_GL_VERSION_OVERRIDE=3.3
      - CYCLONEDDS_URI=.ros/cyclonedds.xml
    ports:
      - "5900:5900"
      - "6080:6080"
    volumes:
      - ./rbe_capstone:/root/ws/src/rbe_capstone
      - ./start_vnc.sh:/root/start_vnc.sh
    stdin_open: true
    tty: true